# Standalone local workflow.
# This file is intentionally identical to the generated workflow so copy-paste and generated setups stay aligned.
name: Deploy to Pockethost

on:
  push:
    branches:
      - main
      - master
      - staging
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pockethost-deploy-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'staging' && 'staging' || 'production' }}
    env:
      POCKETHOST_SFTP_USERNAME: ${{ secrets.POCKETHOST_SFTP_USERNAME }}
      POCKETHOST_SFTP_PRIVATE_KEY: ${{ secrets.POCKETHOST_SFTP_PRIVATE_KEY }}
      POCKETHOST_INSTANCE_NAME: ${{ vars.POCKETHOST_INSTANCE_NAME != '' && vars.POCKETHOST_INSTANCE_NAME || secrets.POCKETHOST_INSTANCE_NAME }}
      HEALTHCHECK_BASE_URL: ${{ vars.HEALTHCHECK_BASE_URL }}
      GITHUB_REF_NAME: ${{ github.ref_name }}

    steps:
      - name: Checkout current commit
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Validate project and deploy configuration
        run: npx pocketbase-pockethost doctor --strict --for deploy

      - name: Run project checks
        run: npx pocketbase-pockethost test

      - name: Deploy project
        run: npx pocketbase-pockethost deploy
