interface:
  display_name: "Pockethost"
  short_description: "Pockethost hosting, CLI automation, health checks, and SFTP deployment guide"
  default_prompt: "Use $pockethost to create or update a Pockethost project with the pocketbase-pockethost CLI, local GitHub Actions workflow generation, SFTP deployment, environment conventions, and health checks."
