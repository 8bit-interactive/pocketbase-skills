name: Deploy SFTP demo site

on:
  push:
    branches:
      - main
      - staging
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/pockethost-deploy.yml
    with:
      working-directory: demo
    secrets: inherit
