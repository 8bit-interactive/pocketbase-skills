# PocketBase PocketHost demo

This is the zero-build demonstration site for the `pocketbase-pockethost` Go CLI.

It is deployed by pushes to `staging` and `main` through the repository workflow. Configure the `staging` and `production` GitHub Environments with the SFTP username, Ed25519 private key, host fingerprint, and matching PocketHost instance names before enabling the workflow.
